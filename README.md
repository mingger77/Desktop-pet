# Desktop Pet 桌面宠物

基于 PySide6 的桌面宠物应用。无边框透明置顶窗口，使用 PNG 精灵图展示女仆角色，通过状态机驱动交互和动画。

---

## 项目结构

```
Desktop pet/
├── main.py              # 主程序，单文件应用（DesktopPet QWidget 类）
├── config.json          # AI 聊天配置（API Key、Base URL、Model）
├── requirements.txt     # Python 依赖（PySide6）
├── CLAUDE.md            # Claude Code 项目指引
├── images/              # PNG 精灵图目录
│   ├── maid--stand.png      # 待机
│   ├── maid--lazy.png       # 懒散
│   ├── maid--cute.png       # 可爱
│   ├── maid--scaried.png    # 受惊
│   ├── maid--apologize.png  # 道歉
│   ├── maid--stone.png      # 石头（游戏出拳）
│   ├── maid--scissors.png   # 剪刀（游戏出拳）
│   ├── maid--paper.png      # 布（游戏出拳）
│   ├── maid--win.png        # 胜利
│   ├── maid--lose.png       # 失败
│   ├── maid--draw.png       # 平局
│   └── maid--work.png       # 工作
├── .venv/               # Python 虚拟环境
└── dist/                # Nuitka 打包输出目录
```

## 功能介绍

### 桌面宠物

桌面上有一只女仆形象的小宠物，窗口无边框、可穿透点击、始终置顶显示。你可以随意拖动她到桌面的任何位置，她会在原地待机，偶尔轻轻浮动。

### 点击互动

- **左键单击**宠物：她会根据当前状态做出随机反应。待机时通常是可爱或受惊的表情，懒散时会道歉。每次反应持续约 1.5 秒后自动恢复。
- **拖拽**宠物：按住左键拖动即可移动她的位置。
- **右键单击**：弹出功能菜单。

### 空闲状态

如果你连续 10 分钟没有点击宠物，她会切换到懒散状态，表现出慢悠悠的浮动动画。点击她即可唤醒。

### 石头剪刀布

在右键菜单中选择「游戏」，宠物会邀请你玩石头剪刀布。

- 弹出粉色主题的选择面板，点击石头、剪刀或布
- 宠物会随机出拳，展示 1.5 秒
- 根据胜负结果，宠物会展示胜利、失败或平局的表情，持续 2 秒
- 点击「退出游戏」可随时结束

### 跳舞

在右键菜单中选择「跳舞」，宠物会跳一段可爱的舞蹈：可爱 → 胜利 → 布 → 平局，每个动作持续 1 秒，整套循环 3 遍后自动结束。

### AI 聊天

在右键菜单中选择「工作」，宠物会化身为 AI 助手「女仆酱」。

- **首次使用**：需要配置 API Key 和 Base URL（支持 OpenAI 兼容接口），以及选择模型名称。配置一次后永久保存。
- **配置好后**：可以选择「开始聊天」进入对话，或「修改配置」更改 API 信息。修改时 API Key 仅显示前 6 个字符，保障安全。
- **聊天界面**：宠物切换到工作状态，弹出粉色调的聊天窗口。你可以打字和女仆酱对话，她会以温柔贴心的风格回复你。API 超时时间为 120 秒。
- 聊天过程中可通过「关闭」按钮结束对话。

### 退出

右键菜单中选择「退出」即可关闭程序。
